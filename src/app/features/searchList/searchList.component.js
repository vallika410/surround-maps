import viewModel from './searchList.view';
import template from './searchList.template.html';

const component = {
    name: 'search-list',
    prop: {
        template,
        viewModel
    }
};

export default component;